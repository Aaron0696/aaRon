---
title: "Template Title"
author: "Your Name"
date: "The Date"
output:
  html_document: 
    toc: yes
    toc_float: yes
    highlight: haddock
    code_folding: show 
---

# Setup

Load data and packages first.

```{r, warning = FALSE, message = FALSE, include = TRUE}

```

# Scale Description

A table that displays the **variable names** and their **description** side-by-side.

```{r}
datatable(var_overview[grep("SWLS", temp$VarName),])
```

# Correlation Matrix

Graphical display of the correlations within the scale. A good scale should show "triangular" patterns of positive correlations.

```{r}

```

# Cronbach Alphas

Calculate Cronbach Alpha using `alpha()` from the `psych` package.

```{r}

```

# CFA

Conduct a CFA to assess the factor structure and *Unidimensionality* using `cfa()` from the `lavaan` package.

```{r}

```
